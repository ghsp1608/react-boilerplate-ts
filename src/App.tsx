import { Routes } from "react-router-dom";
import "./App.css";
import { generateRoutes } from "./utils/helpers";
// import { routes } from "./routes";

// TODO
// add 404 error at rrd
// add error at rrd
// add private route
// add error boundary
// add zustand
// add alias

function App() {
  return <>{/* <Routes>{generateRoutes(routes)}</Routes> */}</>;
}

export default App;
